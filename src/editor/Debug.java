package editor;

import engine.GameObject;
import imgui.ImGui;
import renderEngine.Window;

public class Debug {
    /*
    private GameObject activeGameObject = null;
    private static int fps = 0;

    public void imgui(){
        ImGui.begin("Debug");

        //ImGui.text("FPS: " + String.valueOf(fps));

        ImGui.separator();

        if(Window.get().getHierarchyWindow().getSelectedGameObject() != null){
            activeGameObject = Window.get().getHierarchyWindow().getSelectedGameObject();
            activeGameObject.debug();
        }

        ImGui.end();
    }

     */
}
